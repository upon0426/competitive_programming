#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); ++i)
using namespace std;
typedef long long ll;

int main(void)
{
    int k, x;
    cin >> k >> x;
    if (k*500 >= x) cout << "Yes" << endl;
    else cout << "No" << endl;
}
